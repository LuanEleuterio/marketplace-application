<!DOCTYPE html>
<html>
<head>
    <meta charset='utf-8'>
    <meta http-equiv='X-UA-Compatible' content='IE=edge'>
    <title>Marketplace - Profile Info</title>
    <meta name='viewport' content='width=device-width, initial-scale=1'>
    <link rel='stylesheet'  href='./css/user.css'>
    <link
        href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500&display=swap"
        rel="stylesheet"
    />
</head>
<body>
    <main class="container">
        <div>
            <div class="itens-order">
                <% if(orders.length > 0) {
                    for( let order of orders){ %>
                        <div class="order">
                            <div class="order-info">
                                <div>
                                    <p>PEDIDO REALIZADO</p>
                                    <span><%=order.createdAt%></span>
                                </div>
                                <div>
                                    <p>TOTAL</p>
                                    <span>R$<%=order.charge.amount%></span>
                                </div>
                                <div>
                                    <p>PEDIDO Nº</p>
                                    <span><%=order._id%></span>
                                </div>
                            </div>
                            <div class="order-info-product">
                                <div class="order-info-product-img">
                                    <img src="<%=order.product.img_url%>" alt="">
                                </div>
                                <div class="prod-partner-info">
                                    <p><%=order.product.name%></p>
                                    <p><%=order.partner.name%></p>
                                </div>
                            </div>
                        </div>
                    <%}
                }%>
            </div>
        </div>
    </main>
    <script src="js/user.js"></script>
</body>
</html>